define ['models/infra', 'models/core', 'models/user', 'use!Handlebars', 'use!BackboneFormBS', 'use!BackboneFormsEditors'],
  (Infra, Core, User) ->

    class ScheduleView extends Backbone.Model



    configure = (instrument) ->

    configureSchedule: configure
    ScheduleView: ScheduleView
