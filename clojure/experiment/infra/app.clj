(ns experiment.infra.app)

;; Define a skeleton for a rich-client app
;; - Widget tree
;; - Context-based renderers (search, web, mobile)
;; - Define URL dispatch logic (what data is managed)
;;   - Identify objects to pass to renderer

